import { UIDemo } from "@/components/examples/ui-demo"

export default function UIDemoPage() {
  return <UIDemo />
}

export const metadata = {
  title: "UI Components Demo",
  description: "Interactive demonstration of enhanced UI components including confirmation dialogs, toast notifications, loading states, and empty states.",
}